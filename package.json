{
  "name": "gitboard-action",
  "version": "1.0.0",
  "scripts": {
    "build": "ncc build main.ts -o main && ncc build post.ts -o post && ncc build pre.ts -o pre"
  },
  "dependencies": {
    "@codeaim/gitboard-api": "^0.0.68",
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0",
    "typescript": "^4.9.4"
  }
}
